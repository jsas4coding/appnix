# Generated by Tasker - DO NOT EDIT
# Source: .tasker/config.yml + .tasker/tasks/*.yml
# Regenerate with: tasker generate

version: "3"
dotenv:
    - .env
    - .env.local
    - .env.production
    - .env.test
vars:
    PROJECT_NAME: appnix
    PROJECT_ROOT: '{{.ROOT_DIR}}'
tasks:
    appnix:dev:all:
        desc: Build all apps and generate desktop entries
        summary: Build and integrate - Build all apps and generate desktop entries
        cmds:
            - appnix all
        preconditions:
            - msg: 'Task appnix:dev:all requires ENV=dev (current: $ENV)'
              sh: test -z "$ENV" || test "$ENV" = "dev"
    appnix:dev:build:
        desc: Build AppImages for all configured apps
        summary: Build all apps - Build AppImages for all configured apps
        cmds:
            - appnix build
        preconditions:
            - msg: 'Task appnix:dev:build requires ENV=dev (current: $ENV)'
              sh: test -z "$ENV" || test "$ENV" = "dev"
    appnix:dev:desktop:
        desc: Generate .desktop entries for all configured apps
        summary: Desktop entries - Generate .desktop entries for all configured apps
        cmds:
            - appnix desktop
        preconditions:
            - msg: 'Task appnix:dev:desktop requires ENV=dev (current: $ENV)'
              sh: test -z "$ENV" || test "$ENV" = "dev"
    appnix:dev:install:
        desc: Build and install a single app by name
        summary: Install app - Build and install a single app by name
        cmds:
            - appnix install {{.CLI_ARGS}}
        preconditions:
            - msg: 'Task appnix:dev:install requires ENV=dev (current: $ENV)'
              sh: test -z "$ENV" || test "$ENV" = "dev"
    appnix:dev:list:
        desc: Show all installed AppNix applications
        summary: List apps - Show all installed AppNix applications
        cmds:
            - appnix list
        preconditions:
            - msg: 'Task appnix:dev:list requires ENV=dev (current: $ENV)'
              sh: test -z "$ENV" || test "$ENV" = "dev"
    appnix:dev:reinstall:
        desc: Uninstall and rebuild a single app (kills running process)
        summary: Reinstall app - Uninstall and rebuild a single app (kills running process)
        cmds:
            - appnix reinstall {{.CLI_ARGS}}
        preconditions:
            - msg: 'Task appnix:dev:reinstall requires ENV=dev (current: $ENV)'
              sh: test -z "$ENV" || test "$ENV" = "dev"
    appnix:dev:setup:
        desc: Compile TypeScript and install AppNix CLI to ~/.config/appnix
        summary: Setup CLI - Compile TypeScript and install AppNix CLI to ~/.config/appnix
        cmds:
            - npm run setup
        preconditions:
            - msg: 'Task appnix:dev:setup requires ENV=dev (current: $ENV)'
              sh: test -z "$ENV" || test "$ENV" = "dev"
    appnix:dev:uninstall:
        desc: Remove an installed app and its desktop entry
        summary: Uninstall app - Remove an installed app and its desktop entry
        cmds:
            - appnix uninstall {{.CLI_ARGS}}
        preconditions:
            - msg: 'Task appnix:dev:uninstall requires ENV=dev (current: $ENV)'
              sh: test -z "$ENV" || test "$ENV" = "dev"
    default:
        desc: Show available tasks
        cmds:
            - task --list
        silent: true
    lint:dev:check:
        desc: Run Biome linter and formatter checks
        summary: Lint check - Run Biome linter and formatter checks
        cmds:
            - npx biome check .
        preconditions:
            - msg: 'Task lint:dev:check requires ENV=dev (current: $ENV)'
              sh: test -z "$ENV" || test "$ENV" = "dev"
    lint:dev:fix:
        desc: Run Biome linter with auto-fix
        summary: Lint fix - Run Biome linter with auto-fix
        cmds:
            - npx biome check --fix .
        preconditions:
            - msg: 'Task lint:dev:fix requires ENV=dev (current: $ENV)'
              sh: test -z "$ENV" || test "$ENV" = "dev"
    lint:dev:format:
        desc: Format source files with Biome
        summary: Format code - Format source files with Biome
        cmds:
            - npx biome format --write .
        preconditions:
            - msg: 'Task lint:dev:format requires ENV=dev (current: $ENV)'
              sh: test -z "$ENV" || test "$ENV" = "dev"
    npm:dev:clean:
        desc: Remove compiled output directory
        summary: Clean dist - Remove compiled output directory
        cmds:
            - npx rimraf dist
        preconditions:
            - msg: 'Task npm:dev:clean requires ENV=dev (current: $ENV)'
              sh: test -z "$ENV" || test "$ENV" = "dev"
    npm:dev:compile:
        desc: Compile TypeScript source to JavaScript
        summary: Compile TypeScript - Compile TypeScript source to JavaScript
        cmds:
            - tsc && tsc-alias
        preconditions:
            - msg: 'Task npm:dev:compile requires ENV=dev (current: $ENV)'
              sh: test -z "$ENV" || test "$ENV" = "dev"
    npm:dev:install:
        desc: Install Node.js dependencies
        summary: Install dependencies - Install Node.js dependencies
        cmds:
            - npm install
        preconditions:
            - msg: 'Task npm:dev:install requires ENV=dev (current: $ENV)'
              sh: test -z "$ENV" || test "$ENV" = "dev"
    tasker:export:
        desc: Export resolved config as Tasker.json
        summary: Export - Export resolved config as Tasker.json
        cmds:
            - tasker export
    tasker:generate:
        desc: Generate Taskfile.yml, Makefile, and Tasker.json
        summary: Generate - Generate Taskfile.yml, Makefile, and Tasker.json
        cmds:
            - tasker generate
    tasker:init:
        desc: Scaffold a new Tasker project
        summary: Init - Scaffold a new Tasker project
        cmds:
            - tasker init
    tasker:list:
        desc: Show structured task list
        summary: List - Show structured task list
        cmds:
            - tasker list
    tasker:validate:
        desc: Validate Tasker configuration
        summary: Validate - Validate Tasker configuration
        cmds:
            - tasker validate
    tasker:version:
        desc: Print version information
        summary: Version - Print version information
        cmds:
            - tasker version
    test:dev:coverage:
        desc: Run unit tests with coverage report
        summary: Test coverage - Run unit tests with coverage report
        cmds:
            - npx vitest run --watch=false tests/unit --coverage
        preconditions:
            - msg: 'Task test:dev:coverage requires ENV=dev (current: $ENV)'
              sh: test -z "$ENV" || test "$ENV" = "dev"
    test:dev:e2e:
        desc: Run end-to-end tests with Vitest
        summary: E2E tests - Run end-to-end tests with Vitest
        cmds:
            - npx vitest run --watch=false tests/e2e
        preconditions:
            - msg: 'Task test:dev:e2e requires ENV=dev (current: $ENV)'
              sh: test -z "$ENV" || test "$ENV" = "dev"
    test:dev:unit:
        desc: Run unit tests with Vitest
        summary: Unit tests - Run unit tests with Vitest
        cmds:
            - npx vitest run --watch=false tests/unit
        preconditions:
            - msg: 'Task test:dev:unit requires ENV=dev (current: $ENV)'
              sh: test -z "$ENV" || test "$ENV" = "dev"
    test:dev:watch:
        desc: Run unit tests in watch mode
        summary: Watch tests - Run unit tests in watch mode
        cmds:
            - npx vitest watch tests/unit
        preconditions:
            - msg: 'Task test:dev:watch requires ENV=dev (current: $ENV)'
              sh: test -z "$ENV" || test "$ENV" = "dev"
